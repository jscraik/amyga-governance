# Pack: swift-core

Applies when the repository contains Swift sources.

Required:
- Keep `.swift-format` and `.swiftlint.yml` at the repo root (or document approved overrides).
- Ensure Swift formatting and linting rules stay aligned with CI.
