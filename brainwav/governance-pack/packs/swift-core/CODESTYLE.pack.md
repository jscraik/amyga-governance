# Pack: swift-core

Swift style baseline:

- Prefer `async/await` over completion handlers for new code.
- Use explicit error types and handle failures at boundaries.
- Keep functions small and single-purpose; avoid nested control flow.
