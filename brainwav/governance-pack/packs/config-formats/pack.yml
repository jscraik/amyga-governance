id: config-formats
version: 1.0.0
status: stable
description: YAML/TOML/JSON governance pack (linting, schema validation, deterministic formatting, waiver discipline).
applies_to:
  - yaml
  - toml
  - json
requires:
  - node>=24
waiver_model: uniform (reason + ticket + expires|adr)
commands: commands.yml
nx_targets_suggested:
  - config:lint
  - config:schema:check
