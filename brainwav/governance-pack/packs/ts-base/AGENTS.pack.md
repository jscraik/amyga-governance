# Pack: ts-base

Applies when the repository uses TypeScript. This pack adds baseline TypeScript governance requirements.

Required:
- TypeScript configuration (`tsconfig.json` or `tsconfig.base.json`).
- `typescript` dependency in `package.json`.

Local overrides must remain tightening-only and live in `.local.md` or `.agentic-governance/overlays/`.
