# Pack: ts-base

TypeScript code style applies to repositories using TS. Keep the following in place:

- Named exports only.
- Explicit types at boundaries; avoid `any` except justified shims/tests.
- Maintain ≤40 lines per function and guard-clauses over pyramids.
